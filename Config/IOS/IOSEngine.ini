; Cinderline owns its room/connection protocol in UCinderOnlineSubsystem via
; WebSockets. It does not use Game Center or StoreKit. Do not initialize the
; engine's default IOS service and its MarketplaceKit distributor lookup.
[OnlineSubsystem]
DefaultPlatformService=Null

[OnlineSubsystemIOS]
bEnabled=false
