# P1.3 verification notes

The first Release build succeeded and 20 existing suites passed. The new suite had two invalid fixture assumptions: Striker physical spacing exceeds Tight48 once the 10-unit margin is included, and a turning unit was sampled after leaving the wall. A Scout nominal-spacing fixture and an interpolated wall-centerline check corrected those assertions while retaining footprint/arrival gates.

Review and transient regressions found real gaps: air Hold swept clearance, ground Hold third-party corrections, retained-path blocked reporting/release recovery, and stale facing capture after mouse position was unavailable on release. These were repaired without changing ordinary moving-friendly compression. The 15-case formation suite then passed.

The first scale check incorrectly sampled Defend arrival orientation at 20 units; Defend uses its existing 5-unit anchor tolerance. Tightening that sampling exposed a real 16-unit Wide Defend jam: Mortar 47 remains158 units from goal after 15 other defenders arrive. Its exact source state is `formation-jam-16-2.cinder`. The saved state is now an immutable regression fixture. Defend can yield to friendly traffic and reclaims its unchanged anchor; only explicit Hold remains hard anchored. Combat-facing is restored toward a validated target even during cooldown. All 16 dedicated cases and the 16/160/200 three-leg scale workload now pass, including exact state, trajectory and recording repeats. All failed logs are retained. The old production-traffic test now checks yielding and stable anchor reclamation; explicit Hold checks stay exact. It retains movement-step limits, route bounds, original-goal arrival, save/load and network state checks. All 21 Release and optimized sanitizer suites pass, including the extended four-tick displaced-cooldown facing regression.

P0 paid-four-player noncombat isolation remains separately incomplete in `../patrol-escort/staging-attempt.json`. Do not label that workload passed or physical performance accepted.

Final local verification passed: 35 current-worker server tests, Mac build, 40 Unreal tests (only the two allowed idevice_id host warnings), two actual loopback tests, complete formation/queue/Patrol/Escort/navigation repeats and 12 individually viewed captures. `verification.json` records the exact 159 source inputs and binaries. This is local feature evidence only. Work is stopped after P1; P2 was not started.
