# P1.2 verification notes

- First Release build completed; 19/20 CTest suites passed. The new suite exposed Patrol Return requiring an occupied exact center and two initial fixture setup problems. The original HQ-work-area delivery case was retained separately instead of being removed by the fixture repair.
- First server run passed 35/35 tests against the first protocol-9 worker. This is historical until the final core freeze is retested.
- The first large synthetic workload stopped on a benchmark sampling assertion. The diagnostic run linked the same first Release core and recorded Scout id 181 at tick 361: distance to endpoint 19.9393 before reversal, 32.171 after, displacement 12.25 = speed * Step. The unit legitimately spent the rest of the tick moving away after arrival. The benchmark now accepts a demonstrated pre-tick arrival or the corresponding one-step post-tick bound; no destination, unit count or required lap count was changed. Failed runs are retained.
- Heavy work is serialized. Builds are capped at two compiler jobs. No platform package, device installation, native input, public deployment or thermal acceptance is recorded by these local runs.
- Second Release passed 19/20 suites. The remaining Patrol case had died under fire from its fixture Scout while return fire was intentionally suppressed; the fixture now uses a non-damaging target while retaining endpoint, fog-loss, suppression and fresh-anchor checks. The unchanged Anchor case exposed a returning worker still inside its serviced ore's collision radius. Returning Gather now exits that resource contact with dynamic collision checks before planning the same depot job.
- The second Escort-16 workload failed convergence for one Bastion (14/15 followers arrived). Diagnostics showed repeated yielding across an idle combat leader. The new radial escape was restricted to active Gather/Construct leaders, leaving ordinary Escort avoidance intact. Third Release passes all 20 suites; larger workload and engine verification remain separate gates.
- Subsequent full workloads exposed settled Escorts blocking late followers, reciprocal leader/follower yielding, and a repeated collision correction that erased local progress. Failed third through seventh workload results and neighbor diagnostics are retained under numbered filenames. Fixes preserve immutable destinations, all recipients, the original 9,600-tick limit and arrival criteria. A dedicated traversal regression was added. These diagnostic iterations are not accepted workload evidence.

## Final local gates and workload isolation

The final P1.2 core passes 20 Release/20 ASan-UBSan suites, 35 server tests, the Mac build, 39 Unreal checks, two actual loopback tests, all six sustained workloads, queue/navigation repeats, and eight reviewed render captures. The first paid-four-player final check stopped at tick 44196 when a worker dealt three damage; every measured combat unit remained under Move. Review traced this to the benchmark allowing workers to complete Gather after preparation ended. `Tools/MatchBaseline.cpp` now keeps only workers under holding Move throughout the isolated march, with separate command counts, attacker Weapon diagnostics and `reload_valid=not_checked` when no reload was requested. Core gameplay remains unchanged; original receipts and the old benchmark executable are preserved. Corrected workload binary is archived separately under `Saved/P1SustainedVerified/WorkloadCorrection`. Fresh results are pending.

The worker-only correction exposed a second harness assumption at march tick84: team0 Veil286 had completed its Move and legitimately attacked team3 Needle479, which was still returning from (836,1299) toward its own northern territory. The exact pre/post saves are `march-isolation-march-start.save` and `march-isolation-march-end.save`. An experimental simultaneous1400-radius staging gate never became ready by72000 preparation ticks under the continually changing holding clouds; its failed primary was retained in logs, and the redundant repeat was stopped. The gate was removed from the live tool rather than presented as a gameplay fix. Paid-four-player noncombat isolation remains an open benchmark acceptance issue. No combined P1.2 success receipt is issued.
