services:
  game-server:
    build:
      context: ..
      dockerfile: Server/Dockerfile
    environment:
      HOST: 0.0.0.0
      PORT: 8787
    ports:
      - "127.0.0.1:8787:8787"
    restart: unless-stopped
    init: true
