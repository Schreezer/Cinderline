# Cinderline task board

Current focus, updated 2026-09-13: **Code quality review and focused refactors are complete.** Shared simulation rules, explicit army destination modes, a separate server worker bridge and consistent build/test tooling are verified. All eight portable suites, 32 Node tests, 43 verification-tool tests, 12 deterministic baseline comparisons and 39 Unreal checks pass across three independent review rounds. Protocol 7, save version 10 and visual layout remain unchanged. See [CODE_QUALITY_PASS.md](CODE_QUALITY_PASS.md).

Latest prepared package: global command/allocation executable `bb634a6de18cda60d75c7689e9beacbb3409224c7951a62544e48644d285a395` at `Saved/Packages/IOS/Cinderline.app`, bundle size 494,781,655 bytes. Its SDK 27 IOS/arm64 build, assembly, package, strict signature/profile, IoStore and five exact-binary native iOS-on-Mac cases pass. The bounded 956 x 440 Mac GPU sample measured mean 10.780 ms, median 10.413 ms and p95 13.228 ms, compared with the canyon baseline at 9.706, 9.321 and 14.997 ms; this is not phone heat proof. The package has not been installed or launched on AEON. The installed AEON baseline remains MetalFX `594f5a62...`, installed successfully and left unlaunched. See [COMMAND_ALLOCATION_PASS.md](COMMAND_ALLOCATION_PASS.md) and [the package receipt](../artifacts/ios-device/package-verification.json).

## NOW
- Completed the code quality pass. Removed duplicated game-rule validation, consolidated controller destination state, separated worker transport from room policy, fixed malformed acknowledgement handling and repaired standard verification coverage/engine selection. Verification and remaining architectural follow-ups are recorded in [CODE_QUALITY_PASS.md](CODE_QUALITY_PASS.md).
- Four-player free-for-all is implemented and locally verified alongside the existing 1v1 format across Internet and LAN. Protocol 7 carries two or four seats, private fog and elimination state; a four-player room waits for every seat, and losing every Anchor, surrender, leave or disconnect expiry eliminates one player while survivors continue. All eight native suites, 27 Node tests, 40 Unreal tests, the default worker validation, native client compile, final Mac build, four responsive lobby/panel cases and a reviewed four-faction battlefield fixture pass. No protocol 7 public Internet server was deployed or tested end to end over public WSS, no iOS package was built or installed, and AEON, a physical four-device match, device performance, thermals and full four-human balance remain unverified. See [FOUR_PLAYER_PASS.md](FOUR_PLAYER_PASS.md) and the [combined verification record](../artifacts/four-player/verification.json).
- LAN hosting and online-server hardening are implemented and locally verified. The explicit `npm run start:lan` mode binds private IPv4, prints manual WebSocket URLs and advertises protocol 6 through Bonjour; online mode remains loopback-only behind a trusted TLS proxy. Strict admission, worker, Origin and proxy configuration, readiness drain and bounded container resources are wired. The final Mac build, all 22 Node tests, Unreal private-IP transport, live exact Bonjour discovery, recovery policy, a two-client protocol 6 match on one Mac, and five reviewed responsive panel cases pass. No iOS package was built or installed, and physical-device LAN behavior, another-machine/router behavior, public deployment, performance and thermals remain unverified. See [ONLINE_LAN_PASS.md](ONLINE_LAN_PASS.md) and the [verification record](../artifacts/online-lan/verification.json).
- Short, Standard and Long solo presets are implemented and locally verified at world sizes 3600, 4800 and 6000. Short uses 80% build, train and research times with 42,000 finite ore; Standard preserves the established 4800 behavior with 70,400 ore; Long provides 105,600 ore and more travel space. Active bounds now drive maps, fog, navigation, placement, AI landmarks, saves and snapshots. Save version 9, protocol 6, all seven portable suites, 5,368 match assertions, the Mac build, 33 Unreal tests, seven Node tests and eight reviewed renders pass. Hosted online play remains Standard. No new iOS package was built or installed, and AEON, physical input, device performance, thermals, public protocol 6 deployment and human pacing remain unverified. See [MATCH_LENGTH_PASS.md](MATCH_LENGTH_PASS.md) and the [verification record](../artifacts/match-length/verification.json).
- Selection, rally and new-worker control fixes are implemented and locally verified: press intent works in both directions, normal building-ground taps deselect, DESELECT preserves orders and squads, current and future combat producers inherit the shared army rally with explicit local overrides, and new Drudges use a budgeted reachable-ore plan that keeps paid completed items ready until planning succeeds. Save version 8 and network protocol 5 carry and validate the new state. The final Mac build, 33 Unreal tests, all six portable suites, affected post-validator Network 5/5 and Production Allocation 7/7 reruns, and 22 reviewed render cases pass; Node 7/7 passed before the final validator-only change. The edge-clipped noninteractive `LOCAL RALLY` caption remains a cosmetic follow-up. AEON packaging, physical/device/thermal checks and matching protocol-5 server deployment remain open. See [ARMY_RALLY_CONTROL_PASS.md](ARMY_RALLY_CONTROL_PASS.md) and the [verification record](../artifacts/army-rally-control/verification.json).
- The battle HUD overhaul is implemented and Mac verified: a permanent left icon rail, slim resource chips, bottom-left minimap, illustrated unit ribbon, compact context controls and responsive 336/416-unit palettes. The Mac build, 32 Unreal tests, 25 main HUD renders and 15 tutorial renders pass with full visual review; tutorial `Action + Argument + EntityId`, 44-unit targets, gameplay and safe-area behavior are preserved. The idle fixture's input-blocking rectangle union fell from 31.68% to 18.83%; this is a geometry metric, not pixel coverage or performance. No iOS package was built or installed, and physical touch, performance and thermal behavior remain unverified. See [HUD_OVERHAUL_PASS.md](HUD_OVERHAUL_PASS.md) and the [verification record](../artifacts/hud-overhaul/verification.json).
- Tutorial microsteps are implemented and Mac verified: one state-derived action at a time, concise role introductions, button highlights/arrows, world rings, SHOW focus and recovery from wrong menus, canceled work, abandoned sites or lost units, without outcome shortcuts. The Mac build, 32 Unreal tests and 15 reviewed desktop/phone visual cases pass. The prior command-allocation package remains the latest prepared iOS package. No new package was installed, and physical touch and thermal behavior remain unverified. See [TUTORIAL_MICROSTEPS_PASS.md](TUTORIAL_MICROSTEPS_PASS.md) and the [combined verification record](../artifacts/tutorial-microsteps/verification.json).
- Install and exercise the verified command/allocation package on AEON when authorized, including physical global catalogs, JOBS, building placement, queue/rally overrides and sustained thermal behavior. Protocol 4 passes only against the matching local server and has not been publicly deployed. See [COMMAND_ALLOCATION_PASS.md](COMMAND_ALLOCATION_PASS.md).
- Historical army package `025d6ce3...` passed all five portable suites, six server tests, 23 Unreal tests, 29 Mac gameplay/tutorial renders and four exact-binary native iOS-on-Mac captures. Its roster, three squads, Defend, contextual information and revised HUD are included in the current installed package. Protocol 3 requires a matching server update. See [ARMY_COMMAND_HUD_PASS.md](ARMY_COMMAND_HUD_PASS.md).
- Historical guided-match package `9b61b72e...` passed all 21 Unreal tests, 14 inspected Mac render cases and four exact-binary native iOS-on-Mac states. Its invitation, fourteen objectives and five solo difficulties are included in the current installed package. Human tutorial and touch acceptance remain open. See [GUIDED_MATCH_PASS.md](GUIDED_MATCH_PASS.md).
- Five solo AI difficulties are implemented with persistent selection, strategy differences and compatible save/rematch behavior. The historical `9e362e67...` package passed four portable CTests, 18 Unreal tests and two native iOS-on-Mac menu runs; those changes are included in the current installed package. MetalFX frame interpolation remains inactive. See [AI_DIFFICULTY_PASS.md](AI_DIFFICULTY_PASS.md).
- Navigation implementation complete: narrow-gap routes, reachable work positions, construction access preflight, friendly traffic yielding, bounded recovery and explicit route status. All 27 simulation, 10 navigation and 5 network groups pass, including sanitizers; 17 Unreal tests, six server tests and two-client local transport pass. The 160-unit crossing fixture finishes with all 160 arrivals. See [NAVIGATION_PASS.md](NAVIGATION_PASS.md). Fresh native iOS-on-Mac menu and live battle runs pass, with inspected captures and no new crash reports or runtime assertions.
- The user confirmed historical navigation package `2bf6039a...` worked when it was installed. Physical acceptance for crowded mining/build routes, boundary saturation and reversal, black-edge rendering, a cooled-phone menu and a sustained ordinary match remains open on the current package. The prior map-border package `7c603b00...` retains its own three native iOS-on-Mac edge captures. Historical physical thermal evidence remains tied to `1870dcf9...`. See [NAVIGATION_PASS.md](NAVIGATION_PASS.md), [MAP_BORDER_PASS.md](MAP_BORDER_PASS.md) and [IOS_THERMAL_PASS.md](IOS_THERMAL_PASS.md).
- Mobile HUD implementation and rendered verification are complete. Physical thumb reach, tap/drag/pinch, placement and queue interaction on Aeon remain. See [MOBILE_HUD_PASS.md](MOBILE_HUD_PASS.md).
- Native Designed for iPad on Mac launches pass across the recorded package revisions. Thermal package `4ae4305a...` verified menu rendering, battlefield rendering and thermal presets; earlier revisions verified training, keyboard camera input, guide and pause. The preceding map-border package `7c603b00...` adds three inspected edge captures; those captures remain specific to that package. Automated coordinate clicks still fail with `noWindowsAvailable`; tap, drag, multitouch and construction/production interaction remain unverified. The current MetalFX package is installed on AEON but has not been launched. See [earlier native Mac verification](../artifacts/ios-on-mac/verification.json), [thermal verification](../artifacts/ios-thermal/native-final/verification.json), [map-border verification](../artifacts/map-border/native-verification.json) and [IOS_READINESS.md](IOS_READINESS.md).
- Requested iOS 27 simulator test: boot passed, but installing the current phone app was rejected as IOS/arm64 instead of IOSSIMULATOR/arm64. Fresh simulator compilation passed 22 actions, then linking failed on the missing PLCrashReporter simulator archive. The link still references 785 device-built engine/plugin objects and lacks four simulator libraries. Full simulator engine/dependency build is needed; the current GitHub CLI cannot access EpicGames/UnrealEngine. No game installed or launched on Simulator. Test simulator shut down afterward. See [SDK 27 simulator retry](../artifacts/ios-simulator/sdk27-retry/verification.json) and [IOS_READINESS.md](IOS_READINESS.md).
- Historical navigation package `2bf6039a...` had a verified physical menu. MetalFX package `594f5a62...` is installed but unlaunched; command/allocation package `bb634a6d...` is prepared and has not been installed. Boundary, both landscape orientations, native touch, training, construction, production, full-skirmish and sustained-thermal checks remain open. Prepared `d991e646...` is historical and must not be installed; thermal package `4ae4305a...` retains historical five-launch evidence but was superseded. [IOS_READINESS.md](IOS_READINESS.md) records the exact package boundaries.
- Obtain/build simulator-compatible Unreal engine binaries and missing dependencies. The optional iOS component is recovered and verified, and all 22 simulator compile actions pass. Linking fails because the installed engine supplies physical-iOS Core objects and lacks four simulator archives. No simulator launch or touch test has occurred. Evidence: [IOS_READINESS.md](IOS_READINESS.md).
- Choose a persistent server/domain and deploy private online rooms, then play a human-versus-human match across separate networks. Implementation and expanded Unreal tests over loopback/public WSS are complete. Exact scope, deployment and remaining acceptance: [MULTIPLAYER_PASS.md](MULTIPLAYER_PASS.md).
- Play the complete guided training with physical input and assess pacing, FIND/MORE, construction and waypoint clarity. Implementation, all eight Unreal tests, desktop/compact rendering and keyboard pause/restart checks are complete. Scope and remaining acceptance: [HELP_TUTORIAL_PASS.md](HELP_TUTORIAL_PASS.md).
- Play a normal skirmish with the new locomotion, work, recoil, hover and terrain pass. Assess readability at normal zoom; [ANIMATION_TERRAIN_PASS.md](ANIMATION_TERRAIN_PASS.md) records the rendered fixtures, short capped performance sample and remaining limits.
- Physically check production queue paging and cancellation beyond the first page. The 20-entry implementation is built and tested; see [PRODUCTION_QUEUE_PASS.md](PRODUCTION_QUEUE_PASS.md).
- Investigate the player's report of hidden terrain appearing after camera panning. Option-drag is physically confirmed working; reproduce the fog symptom in a future playtest. The game is currently closed after the player's fan complaint. See [TRACKPAD_BUILD_FEEDBACK_PASS.md](TRACKPAD_BUILD_FEEDBACK_PASS.md).
- Verify physical Drudge placement and inspect the desktop/compact build-menu and preview labels.
- Verify the Drudge construction fix from the first hands-on session: physical travel, interrupted mining, pause/resume and return to ore. See [CONSTRUCTION_PASS.md](CONSTRUCTION_PASS.md).
- Continue through a full skirmish using [PLAYTEST.md](PLAYTEST.md).

## NEXT
- After physical HUD/touch and sustained-match acceptance, prioritize stable public hosting with protocol 3 and a complete two-device online match. The army/selection/command HUD redesign is delivered; further art and authored Landscape work follow that acceptance. Reference direction and delivered art are in [VISUAL_TARGET_PASS.md](VISUAL_TARGET_PASS.md).
- Complete physical-device touch and gameplay acceptance now that development signing is verified. Keep the remaining simulator engine-build gap separate; see [IOS_READINESS.md](IOS_READINESS.md).
- Finish sustained background cadence and normal-match resume checks when performance testing resumes. The applied idle fix and its measured menu improvement are in [IDLE_GPU_PASS.md](IDLE_GPU_PASS.md); further load tests stopped after the player reported loud fans.
- Extend profiling to sustained late-game and thermal conditions after the current animation/terrain pass. The first visual upgrade is complete in [VISUAL_UPGRADE_PASS.md](VISUAL_UPGRADE_PASS.md); the measured Mac renderer choice and efficiency work are tracked in [METAL_PERFORMANCE_PASS.md](METAL_PERFORMANCE_PASS.md).
- Continue repairing player-interaction findings and assess economy, scouting, counters, expansion and match pacing.
- Listen to combat, interface and production cues together and adjust the in-game mix.
- Inspect the full roster and enemy team palette through gameplay, then tune motion readability, fog edges and late-game rendering.

## LATER
- Expand terrain gameplay only after designing height/navigation rules. The authorized canyon pass now adds visual Landscape relief inside existing blocked areas; it deliberately retains level gameplay and deterministic movement.
- Measured human match pacing toward 20–30 minutes.
- Broader iPhone/iPad device coverage and sustained performance/thermal tests after the first iOS gameplay pass.
- Asymmetric second faction, public matchmaking, accounts/ranking, replay viewer and more content.

## DONE
- Bounded camera overscroll to a 15% visible-footprint margin clamped to 72–360 world units, including the interpolated camera and immediate reversal after a saturated drag. Reduced terrain to the exact 4800 bounds and added four static unlit opaque black exterior planes without changing fog or the approved mobile UI. The Mac build, all 17 game tests, 96 camera cases and seven rendered captures passed. The SDK 27 build/package/signing and three native iOS-on-Mac edge captures also pass; physical-device acceptance remains open. See [MAP_BORDER_PASS.md](MAP_BORDER_PASS.md).
- Completed the local thermal optimization and startup-race repair: removed hidden-world menu rendering on Mac/iOS, cached minimap fog runs, added thermal/Low Power pacing and quality recovery, removed unused Mac translucency-lighting volumes, and repaired iOS module diagnostics. Historical `d991e646...` exposed the race; `4ae4305a...` passes schema 3 build/package/signing, tests and five fresh iOS-on-Mac launches. That historical thermal package was never installed. [IOS_THERMAL_PASS.md](IOS_THERMAL_PASS.md) records the boundaries.
- Installed and launched optimized physical package `1870dcf9ef090b8d0125bd3265ebe5b59d82dff1723f3c800f9e330ef20b12ec` on AEON. Confirmed signed sustained execution, Game Mode opt-in, serious thermal-state detection and the adaptive minimum-quality caps. The game was stopped when the player left. Sustained cooldown, utilization and per-pass attribution remain open. See [IOS_THERMAL_PASS.md](IOS_THERMAL_PASS.md).
- Delivered the menu-only Dynamic Island correction on AEON. The final code reads UIKit insets on the main thread without changing Unreal's global safe area, scopes the corrected Canvas region to menu controls and restores it afterward. Gameplay layout remains unchanged and the art stays full bleed. Mac/iOS builds, 15 focused tests, full SDK 27 packaging and signing passed. The final package installed and launched; a real 2868x1320 capture shows safe controls, and the player called the spacing perfect. Landscape-right menu acceptance needs a new capture; the earlier raw image showed App Library and was excluded. See [VISUAL_TARGET_PASS.md](VISUAL_TARGET_PASS.md) and [IOS_READINESS.md](IOS_READINESS.md).
- Replaced the mobile full-width bottom dock with corner controls, selection-dependent commands, on-demand build/production/type drawers and compact tutorial guidance. Corrected tablet scaling and preserved 44-unit targets and native safe insets. Mac/SDK 27 iOS builds and five Unreal tests pass; nine phone-preview and nine native iOS-on-Mac states pass bounds, overlap and central-world-input checks. Verified native keyboard pause/resume and closed both test apps. See [MOBILE_HUD_PASS.md](MOBILE_HUD_PASS.md).
- Diagnosed the Launcher accepting an iOS TagChange but failing to start it, recovered the exact component with Epic BuildPatchTool, verified all 15,223 files against the original manifest before and after installation, and reconciled the backed-up Launcher component record. Added `build-ios-simulator` with a two-job limit. Real simulator compilation passes; linker/Mach-O diagnostics establish the remaining engine-distribution blocker. Corrected the earlier blanket simulator-support claim. See [IOS_READINESS.md](IOS_READINESS.md).
- Implemented private online 1v1 with room codes, ready synchronization, authoritative shared-rule match workers, per-seat fog-filtered snapshots, opaque IDs, validated commands, interpolation, reconnects, surrender/results and graceful leave. Added a desktop/compact Slate lobby and online menu/help rules that protect the offline save. All four native CTests, six real server integration tests and eight offline Unreal tests pass. The expanded two-client Unreal test passes over loopback and public WSS; the public run logged two existing iOS helper warnings and no errors. Verified rendered room creation, live network play, compact keyboard focus scrolling and clean leave. Deployment files are ready; persistent hosting, human balance and iOS remain separate checkpoints. Test game/server/tunnel closed. See [MULTIPLAYER_PASS.md](MULTIPLAYER_PASS.md).
- Added an in-game field guide with nine topics, desktop/touch instructions and 15 definition-driven references, plus a separate nine-objective practice mission with no attacking AI. Help pauses safely; training protects the skirmish save and supports confirmed restart. Built and passed all eight Unreal tests, including normal-rule tutorial completion, modal/lifecycle/save routing and F1 conflict regression. Inspected desktop/compact screens, paging, camera progression and restart with keyboard input; repaired the compact card focus obstruction and Unreal F1 wireframe conflict. Closed Unreal after capped previews. See [HELP_TUTORIAL_PASS.md](HELP_TUTORIAL_PASS.md).
- Added 18 articulated mesh parts with walking legs, active work tools, recoil and suspension, plus hover/bank for Scout, Mender and Kite. Rebuilt four cliffs with fractured crowns and rubble, and added layered basalt/ash/mineral ground shading. All motion/cliff imports, the Mac build and four Unreal integrations passed. Inspected live walking, mining, construction, combat and terrain captures; the short foreground fixture held the 30 FPS test cap at 2560×1440. Preserved pause, fog and instancing behavior; closed Unreal after testing. See [ANIMATION_TERRAIN_PASS.md](ANIMATION_TERRAIN_PASS.md) and its video/evidence manifest.
- Prepared iOS touch/lifecycle handling: stationary holds no longer issue orders, placement holds preserve the builder, multitouch clears stale selection intent, and app backgrounding pauses until explicit Resume. Added native safe-area padding and guarded iOS packaging helpers. The Mac build and all four headless Unreal integrations passed. The initial missing-component blocker was resolved in the later simulator retry above; [IOS_READINESS.md](IOS_READINESS.md) tracks the current engine-linking blocker, signing and device acceptance separately.
- Expanded production queues to 20 entries per building. The HUD shows totals and paged entries, six per desktop page and eight on compact layouts, with cancellation available at every position. Cost, reserved crew, FIFO order and save/load are covered by a new full-queue regression. All three focused production tests, the Mac build and all four headless Unreal integrations passed. The game remains closed; physical UI acceptance is tracked in [PRODUCTION_QUEUE_PASS.md](PRODUCTION_QUEUE_PASS.md).
- Added desktop frame ceilings of 120 FPS during play, 30 FPS for menu/pause/results and 10 FPS in the background; idle battlefield ticks skip repeated fog/instance work. Built and applied the fresh module, passed all 4 Unreal integrations and verified 91 matching source inputs. Live menu and pause held 30.00 FPS; the short same-menu whole-GPU mean fell from 99.70% to 50.75%. Confirmed minimized cap selection, then closed the game to let the Mac cool down. See [IDLE_GPU_PASS.md](IDLE_GPU_PASS.md) for measurement limits and remaining background cadence checks.
- Added Option-drag camera panning for Mac trackpads, preserved builder selection during placement drags, and aligned build menus/previews with authoritative construction requirements. Crucible rejections now explain T2 research at the Resonator. Passed 26 portable groups, 3 CTests, sanitizers, the Mac build and all 4 Unreal integration tests. Restarted with the user's authorization; the player confirmed camera panning works. Observed the rendered construction HUD; physical placement and full build-menu visual acceptance remain open in [TRACKPAD_BUILD_FEEDBACK_PASS.md](TRACKPAD_BUILD_FEEDBACK_PASS.md).
- Optimized the Mac Metal presentation: native-resolution TAA replaces the costly TSR path, unchanged instance batches skip uploads, moving batches use incremental instance data, and minimap/label submissions do less CPU work. Added longer CPU/GPU diagnostics and actual-component movement/death/replacement regressions. Build, 26 portable groups, 3 CTests and all 4 Unreal integrations passed. See [METAL_PERFORMANCE_PASS.md](METAL_PERFORMANCE_PASS.md) for measured comparisons and remaining limits.
- Upgraded the desktop menu/HUD to crisp Retina rendering, added shadows/AO/bloom/TSR, PBR terrain/model materials, four original Blender cliff meshes and softer fog. Imported and validated all assets; passed 26 portable groups, 3 CTests and 4 Unreal integrations. Inspected desktop/compact scenes and sampled 45.8–47.6 FPS at 2560×1440. See [VISUAL_UPGRADE_PASS.md](VISUAL_UPGRADE_PASS.md) for evidence and remaining art/performance work.
- Added explicit combat events, distinct weapon/healing/impact/destruction visuals, fog-safe coalesced audio and version 4 saves with version 1–3 readers. Passed 26 portable groups, native checks, sanitizers and four Unreal integration tests; inspected desktop/compact combat fixtures and live audio submissions. See [COMBAT_FEEDBACK_PASS.md](COMBAT_FEEDBACK_PASS.md) for evidence and playtest limits.
- Implemented scouting-driven AI: remembered enemy bases, recent army sightings, searches after clearing a base, adaptive paid production and version 3 knowledge persistence with version 1/2 compatibility. Passed 23 portable groups, native checks, sanitizers and all 3 Unreal integration tests. [AI_STRATEGY_PASS.md](AI_STRATEGY_PASS.md) tracks completion, evidence and remaining work.
- Fixed Drudge construction: workers travel, stop mining, build on-site, pause when interrupted and resume prior mining after completion. Added free reassignment, save migration, AI builder reservation and construction state UI. Fresh verification passed 19 rule groups, all 3 CTests, ASan/UBSan and both Unreal integration tests. See [CONSTRUCTION_PASS.md](CONSTRUCTION_PASS.md).
- Created the source structure, original Cinderline design, centralized definitions and command interface.
- Implemented a fixed-step C++ simulation with economy, queues, construction, research, movement, combat, fog, paid AI and saved matches.
- Implemented three map layouts and a first faction with eight units and six buildings.
- Final mechanics verification passed 14 simulation groups, all 3 CTests including 3,000 native render frames, ASan/UBSan and both Unreal integration tests. Exact source hashes and results are recorded.
- Repaired worker final approach, invalid placement, formation destinations, native HUD stability and exact construction health.
- Native UI checks exercised production, selection, building, cancellation, pause, save/load, army movement, natural defeat and rematch.
- UE 5.8.2 Mac Editor builds and launches the generated battlefield with working simulation and HUD.
- Actual Unreal checks covered menu start, ore gathering, arrow pan, Home, wheel zoom, pause/resume and compact menu/battlefield layouts. The user confirmed START SKIRMISH works with their own mouse.
- Unreal reached natural defeat through AI gameplay at 5:30 while the player base was left undefended. This was not a balanced human match test.
- Original generated basalt and menu artwork are imported, with prompts and provenance recorded.

- Fixed cover pursuit, displaced Hold orders and support following. AI now evaluates visible remaining ore, dispatches local defense workers, advances while scouting and attaches new/orphaned Menders to armed leaders. A second review found no remaining important issue in this bounded pass.
- Authored and roundtrip-validated 15 Blender models; all imported into Unreal with exact dimensions, slots and triangle counts. Fixed Skim exhaust geometry before import.
- Integrated shared materials, readable copper ore, ambient lighting, larger fonts and compact feedback bounds. Actual desktop and compact construction/battlefield captures passed.
- Imported eight sound cues and wired interface, order and production feedback. Live interface/rejected-order submissions reached Unreal audio; listening/mix review remains open.
- Built the presentation changes and passed both engine integration tests. [PRESENTATION_PASS.md](PRESENTATION_PASS.md) separates source, runtime and outstanding playtest evidence.

## BLOCKED
- Automated absolute mouse clicks do not update Unreal's cached mouse location on this Mac. Physical mouse start is user-confirmed; command integration tests and keyboard checks provide separate evidence. A Terra high playtest attempt reached 00:55 but could not select units by click or drag; no complete skirmish was played. See [TERRA_SKIRMISH_PLAYTEST.md](TERRA_SKIRMISH_PLAYTEST.md).
- The optional iOS engine files are installed and verified. ARM64 simulator game compilation passes, but linking requires simulator-built Unreal engine objects and missing third-party archives. The later device signing retry succeeded with the existing team, a matching development identity/profile and the registered phone. See [IOS_READINESS.md](IOS_READINESS.md).

Unreal desktop launch and camera navigation are now demonstrated. Physical touch acceptance and the later gameplay checkpoints remain open; source implementation alone does not complete them.
